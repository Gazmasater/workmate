name: Go Test

on:
  push:
    branches: [ iter1 ]
  pull_request:
    branches: [  ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: 1.21
      - run: go test ./... -v